#include <bits/stdc++.h>
#include "random.h"
using namespace std;



int main(int argc, char* argv[]) {
    if(argc != 2) {
        perror("parameters error\n");
        exit(1);
    }
    
    return 0;
}